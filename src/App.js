import React, { Fragment } from 'react';
import './App.scss';
import MapContainer from './components/MapContainer/MapContainer';

const App = () => {
  return (
    <Fragment>
      <MapContainer />
    </Fragment>
  );
};

export default App;
